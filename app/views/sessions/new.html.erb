<h1>Login</h1>

<% if flash.any? %>
	<ul>
	  <% flash.alert.each do |message| %>
	    <li class='flash'><%= message.first.to_s %></li>
	  <% end %>
	</ul>
<% end %>

<%= form_for(:user) do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>
	<br>
	<%= f.label :email %>
	<%= f.text_field :email %>
	<br>
	<%= f.label :password %>
	<%= f.password_field :password %>
	<br>
	<br>

	<%= f.submit 'Login'%>

<% end %>

<br>

or <%= button_to "Log in with Google", '/auth/google_oauth2'  %>
